// Email service is currently disabled
export async function sendPasswordResetEmail(email: string, resetToken: string) {
  console.log('Password reset functionality is currently disabled');
  return false;
}